#include "main.h"

/**
 * print_last_digit - prints the last digit
 * @n: the given number
 * Return: returns the last digit
 */

int print_last_digit(int n)
{
	int n;
	int x;

	if (n <)
	n = -n;
	x = n % 10;
	return (x);
}
